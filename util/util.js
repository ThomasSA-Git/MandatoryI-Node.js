/* import DOMPurify from 'dompurify';

const purify = DOMPurify();

export function sanitizeStringWithTableRows(tableRows) {
  const sanitizedHTML = purify.sanitize(tableRows);

  return sanitizedHTML;
  } */